# 核心依赖 - 兼容Python 3.8.2
pandas==1.3.5
numpy==1.22.4
requests==2.28.2
matplotlib==3.5.3
pyyaml==5.1.2
python-dateutil>=2.8.2
pathlib>=1.0.1

# 数据获取与处理 - 兼容Python 3.8.2
pandas-market-calendars==4.3.1
pytz==2023.3

# 日志管理 - 兼容Python 3.8.2
loguru==0.6.0

# 测试框架 - 兼容Python 3.8.2
pytest==7.2.0
pytest-cov==4.0.0

# 数据导出 - 兼容Python 3.8.2
openpyxl==3.1.2
xlsxwriter==3.0.9

# 开发工具 - 兼容Python 3.8.2
black==22.3.0
flake8==4.0.1
isort==5.12.0

# 注意：系统使用的其他库（如os, sys, logging, json, shutil等）为Python标准库，无需单独安装
# 当前requirements.txt已包含系统所有外部依赖项，确保版本兼容Python 3.8.2